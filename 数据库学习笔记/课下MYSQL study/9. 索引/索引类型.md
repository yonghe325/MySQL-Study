在开发中
一般不使用MYSQL自带的全文索引(不好用)
而考虑使用 : 全文搜索的两个框架 
 Solr 和 ElasticSearch(ES)

# primary key 主键索引
位字段添加主键约束时,自动为该字段添加索引
# unique 唯一索引
当字段被添加唯一约束时,同时也是索引
# index 普通索引 : ==使用最多==
# fulltext 全文索引(适用于MYISAM)


