自连接,笛卡尔集等都是通过where 条件进行过滤
根据==关联条件==,显示所有匹配的记录
==匹配不上的,不显示==
```
# 列出部门名称和这些部门员工名称和工作,同事显示出那些没有员工的部门
SELECT ename ,job,dname
	FROM emp ,dept
	WHERE emp.`deptno` = dept.`deptno`
	ORDER BY dname;
```
![[不使用外连接.png]]
由于dept表中 , deptno = 40 的部门没有员工,上述查询无法查找 ==没有员工的部门==
![[dept.png]]