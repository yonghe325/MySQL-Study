指在同一张表上进行连接查询
 只发生在一张表上
 即 ==将一张表看做两张表==
 需要查询的数据在同一张表上
 ![[Pasted image 20251012083555.png]]
 通过别名将同一张表链接
 ```
 # 显示公司员工和他上级的部门名字
 SELECT worker.`ename` ,boss.`ename` 
	FROM emp AS worker,emp AS boss
	WHERE worker.`mgr` = boss.`empno`;
## 排除了 mgr 或 empno 为空的情况
 ```