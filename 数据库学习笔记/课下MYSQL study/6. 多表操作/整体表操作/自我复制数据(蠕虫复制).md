有时,为了对某个sql语句进行效率测试,我们需要海量数据时
用此方法为表创建海量数据,进行测试
```
INSERT INTO <table_name>
	SELECT * FROM <table_name>;
```
==以2的指数倍进行复制==
```
INSERT INTO tab_copy
	SELECT * FROM tab_copy;
```
注意:
+ 不能自我复制主键
+ 当列数据不能重复时,不能复制