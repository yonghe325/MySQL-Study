# 设置自增长字段
```
字段名 整形 primary key auto_increment
```
# 添加数据
```
INSERT INTO (字段1,字段2,字段3...) values (null,'值2','值3'...);
INSERT INTO (字段2,字段3...) values ('值2','值3'...);
INSERT INTO <表名> values (null,'值2','值3'...);
```
null表示没有给值
实际上是填入 自增长的整形