# 原子性 atomicity
事务是不可分割的工作单位
事务操作要么都发生,要么都不发生
# 一致性 consistency
事务必须使数据库从一个一致性状态变化到另一个一致性状态
# 隔离性 isolation
事务的隔离性是多个用户==并发==访问数据库时，数据库为每一个用户开启的事务，不能被其他事务的操作数据所干扰
多个井发事务之间要相互隔离
# 持久性 durability 
持久性是指一个事务一旦被提交，它对数据库中数据的改变就是==永久性==的，
接下来即使数据库发生故障也不应该对其有任何影响