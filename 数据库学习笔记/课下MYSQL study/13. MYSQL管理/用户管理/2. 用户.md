当项目开发时,可以根据不同的开发人员,赋给他们相应的mysql权限

不同的数据库用户,操作的库和表不相同

# 用户相关语句
## 用户创建
```
create user `用户名`@`允许登录的位置` identified by `密码`;
```
## 删除用户
```
drop user `用户名`@`允许登录的位置`;
```
## 修改用户密码
```
# 修改自己密码
SET PASSWORD = PASSWORD(<密码>)
# 修改别人的密码,需要权限
SET PASSWORD FOR '用户名'@'允许登录的位置' = PASSWORD('<密码>')
```